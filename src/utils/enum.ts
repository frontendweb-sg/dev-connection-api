export enum Status {
    pending = "pending",
    rejected = "rejected",
    approved = "approved",
    active = "active",
    inactive = "inactive",
}

export enum PostStatus {
    public = "public",
    private = "private",
    custom = "custom",
    pending = "pending",
}
